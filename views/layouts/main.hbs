<!DOCTYPE html>
<html>
  <head>
    <title>Just Reading</title>
    <style>
      body { margin: 0; padding-bottom: 3rem; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; }

      #form { background: rgba(0, 0, 0, 0.15); padding: 0.25rem; position: fixed; bottom: 0; right: 0px; display: flex; height: 3.1rem; box-sizing: border-box; backdrop-filter: blur(10px);
      width:285px;border-left: 1px solid black;border-top: 1px solid black; }
      #input { border: none; padding: 0 1rem; flex-grow: 1; border-radius: 2rem; margin: 0.25rem; }
      #input:focus { outline: none; }
      #form > button { background: #333; border: none; padding: 0 1rem; margin: 0.25rem; border-radius: 3px; outline: none; color: #fff; border-top:1px solid black; }

      #messages { display: inline-block; list-style-type: none; margin: 0; padding: 0;width:284px; position:fixed;right:0px; height:84%; border-left:1px solid black; overflow-y: auto;overflow-x: hidden; }
      #messages > li { padding: 0.5rem 1rem;width:285px; }
      #messages > li:nth-child(odd) { background: #efefef; }
      #header {background-color:#333; height:100px; width: 100%;}
      #headerleft { height:100px; width: 85%;display: inline-block;}
      #headerleft h1 {margin:0px;margin-left: auto;margin-right:auto;width:200px;padding-top:25px;color:#fff;}
      #headerright { height:100px; width: 14%;display: inline-block;}
      #headerright h3 {margin:0px;margin-left: auto;margin-right:auto;width:125px;padding-top:25px;color:#fff;}
      #game {width: 85%;display: inline-block; }
      #game h4{ margin-left: auto; margin-right: auto; width:150px;}
    </style>
  </head>
  <body>
      <div id="header">
        <div id="headerleft">
            <h1>Just Reading</h1>
        </div>
        <div id="headerright">
            <h3>Welcome {{player}}</h3>
        </div>
      </div>

      {{{body}}}
  </body>
</html>